<block>
    <view class="{{['index-wrap']}}">
        <view class="map-wrap">
            <block wx:if="{{carUserInfo != ''}}">
                <baidu-map __triggerObserer="{{ _triggerObserer }}"></baidu-map>
            </block>
        </view>
    </view>
</block>